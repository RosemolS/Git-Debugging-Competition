#include <stdio.h>

{
int average(int a, int b)
int avg;
avg=(a+b)/2
}
int average(int c, int d, int e)
{int avg;
avg=c+d+e/2
return avg;
}
int main()
{
    int ans;
    
    ans = avg(4,5);
    printf("Average of the numbers is: %d",ans);

    
}

